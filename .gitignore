node_modules/
# Downloaded PVGIS TMY files (several GB for the full 0.5° grid)
cache/
# Generated grids and power-line tiles. Remove these lines to commit them and publish
# the map as a static site (e.g. GitHub Pages).
public/data/grids/
public/data/gridlines/
public/data/osm-power/
.DS_Store
